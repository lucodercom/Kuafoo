#include "ToolsFormaterImageController.h"

cv::Mat ToolsFormaterImageController::ConvertToTiff(std::vector<cv::Mat> matrix)
{
	return matrix[0];
}

cv::Mat ToolsFormaterImageController::ConvertToOthers(std::vector<cv::Mat> matrix)
{
	return matrix[0];
}
